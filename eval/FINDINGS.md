# Findings

Accumulated evidence from YOUR runs of the loop. Append via `/review` only when the
evidence moves or a process failure cost a real decision. Every entry: date, the
observation with its numbers, what it implies for `/trade`, and the current N.

Nothing here yet — the dataset starts when you do.
