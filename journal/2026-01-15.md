# 2026-01-15 — EXMPL

Status: closed
Committed-at: 2026-01-15 09:58 ET

> **SYNTHETIC EXAMPLE — not a real trade.** `EXMPL` is an invented ticker and every number
> below is made up to show the shape of a completed entry. Delete this file (or keep it as
> a reference) before you start your own journal. Your real entries start from
> `journal/_TEMPLATE.md`.

## Pool context (point-in-time)
scan_date 2026-01-14 → entry day 2026-01-15. Pool size 47 names. Regime:
vix_at_scan 14.2 / vix3m 16.1 / `regime_rail_pass = true` ([[regime-rail-vix-term]] clears —
term structure in contango, no stand-down). Nothing excluded at the pool level; earnings
screened per-candidate below. Run timestamp 09:52 ET (inside the entry window,
[[entry-1000-et]]).

## Candidate
EXMPL $50C expiring 2026-02-20 (call). At scan: delta 0.34, ~11% OTM, overnight_score 6.
Live refresh at 09:52 ET: delta 0.33 (still in band), last mark $1.20 (roughly flat vs the
overnight $1.18 reference — opportunity not yet consumed), OI building since the open. Beat
the shortlist because two higher-score names had live deltas outside 0.20–0.46 after the
open and a third had already ripped +30% from its overnight mark.

## Thesis
Single-leg long call on a delta-calibrated pool ([[delta-band-0-20-0-46]] — live delta 0.33
sits mid-band, the only feature that separated winners from losers). Moneyness ~11% OTM is
inside the preferred bucket ([[moneyness-10-15-otm]]). The setup's edge is *selection +
exit*, not direction — the pool is fairly priced ([[option-pnl-not-underlying]] governs how
this is scored). No same-day-only levers are load-bearing; the plan is a 2–3 day hold, so
no exit-context mismatch. OI is a liveness read only, never quality ([[oi-not-quality-signal]]).

## Liquidity check
Live `get_liquidity` overlay confirmed OI building and a stable last mark; bid/ask/spread is
absent on this data plan (noted, not blocking). Session-frozen scan OI was ignored for
quality per doctrine. Accepted: liveness verified, willing to own the fill risk at this size.

## Exit plan (designed BEFORE entry)
Intended hold: 2–3 days (harvest the late pop, not a same-day scalp). Target +60%, stop
−30%, mandatory time-stop = close of day 3. Rationale: the harvest curve says pops land day
2–3, not day 1, and cheap targets amputate the right tail ([[three-day-harvest-curve]]);
a fixed same-day bracket is the documented value-destroyer ([[fixed-exit-composites-negative]]).
Evidence: `query_outcomes(view="surface")` on the 0.30–0.35 delta bucket showed median peak
~+22% with a fat right tail; `query_outcomes(view="exit_rule", target_pct=60, stop_pct=30,
horizon=3)` scored EV-positive vs tighter brackets (heuristic_share ~0.3, noted).
Management rule: may tighten the stop toward breakeven after a +40% touch (risk-reducing
only); never loosen.

## Sizing
Paper notional: 1 contract (example size). Real-money arithmetic practiced: on a $10,000
example account, one $120 contract that zeroes is ~1.2% of the book — inside a small
fixed-fractional budget ([[position-sizing-basics]]).

## Critic
trade-critic: **PASS**. Objections raised and resolved: (1) "is the +60 target reachable in
3 days?" — the harvest curve base rate says ~31% touch +50% by day 3, accepted as a
feasible-not-reliable target and journaled as such; (2) "earnings risk?" —
`get_signal(view="earnings")` returned next_earnings_date well after the day-3 time-stop,
so no hold-through-earnings exposure ([[earnings-iv-crush]]); (3) strongest counter-argument
— the pool has zero directional alpha, so this rides selection + exit only, which the thesis
states explicitly.

---
(Everything above is immutable once Status: committed. Everything below is appended later.)

## Outcome
Entry 2026-01-15 10:01 ET at $1.20 (mark). Day 1 drifted to $1.10. Day 2 popped to $1.75
(+46%), tightened stop to breakeven per the management rule. Day 3 touched $1.95 (+62%,
target) at 13:20 ET — exited at the target. Option PnL: **+62%**. MFE ~+64%, MAE ~−13%.

## Lessons
Wiki-distill candidates: none new — this entry only reused existing notes. Process quality
(separate from the win): the exit plan was designed before entry from the harvest curve and
held through the day-1 drawdown, which is the intended behavior; the outcome being positive
does not by itself validate anything (a good process can lose). Sample of one, synthetic.
